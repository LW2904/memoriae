<!doctype html>
<html lang="en">
  <head>
    <%- include('partials/head') %>
    
    <title>memoriae</title>

    <style>
    </style>
  </head>

  <body>
    <div class="container">
      <% if (!locals.user) { %>

        <p>
          <b>What?</b> <i>Memoriae</i> is a simple site, allowing users to store information  
          about persons.
        </p>

        <p>
          <b>Why?</b> I have a very bad memory. 
          <small>And someone very important to me inspired me.</small>
        </p>

        <p>
          To check it out, <a href="/login">log in</a> or <a href="/signup">sign up</a>.
        </p>

        <p>
          Go ahead and read the  <a href="https://github.com/lw2904/memoriae">code</a>, 
          or <a href="mailto:fsoc@firemail.cc">contact</a> me.
        </p>

        <P>
          <small class="muted">
            Version <%= version %>, running node <%= node %>.
          </small>
        </P>

      <% } else { %>

        <div class="row">
          <p>
            Welcome, <b><%= user.nickname %></b>.
            <span class="u-pull-right"><a href="/logout">Log out.</a></span>
          </p>
    
          <% user.persons.forEach((person) => { %>
      
            <div class="three columns person">
              <p><%= person.firstName %></p>
            </div>
      
          <% }) %>
        </div>
        
      <% } %>
    </div>
  </body>
</html>
